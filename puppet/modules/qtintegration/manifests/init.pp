class qtintegration {
    # This module deploys the core guts of the Qt CI system.
    # However it depends heavily on some scripts which are currently
    # not available outside of Nokia.  Sorry :(
}


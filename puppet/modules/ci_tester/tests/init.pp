# Check that a ci_tester with default arguments doesn't crash...
# We can't check for absence of warnings because we expect the default
# config to warn about some things which need to be set up manually.
include ci_tester

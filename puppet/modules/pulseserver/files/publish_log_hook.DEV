#!/bin/sh
# development version of the log publish hook.
# Note, ssh username must be configured in ~/.ssh/config manually
exec $HOME/bin/publish_log_hook \
    --ssh-dest-host dev-testresults.qt-project.org \
    --ssh-dest-path /var/www/testresults/ci \
    --verbose \
    "$@"

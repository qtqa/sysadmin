# deployed by puppet
# FIXME: use augeas to edit this file so we do not clobber upstream changes?

[SeatDefaults]
autologin-user=<%= testuser %>
autologin-user-timeout=0
greeter-session=unity-greeter
user-session=ubuntu

